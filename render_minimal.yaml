services:
  - type: web
    name: Konferans Minimal
    env: python
    buildCommand: pip install -r requirements_minimal.txt
    startCommand: gunicorn -k eventlet -w 1 minimal_app:app
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        fromDatabase: false
        value: secret!
