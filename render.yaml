services:
  - type: web
    name: konferans
    runtime: python3
    buildCommand: pip install -r requirements.txt

startCommand: gunicorn konferans.wsgi

    envVars:
      - key: SECRET_KEY
        value: your_secret_key_here
    plan: free
